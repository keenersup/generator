_38f‍.x([["NODE_ENV",()=>NODE_ENV],["MONGO_ROOT_USER",()=>MONGO_ROOT_USER],["MONGO_ROOT_PASSWORD",()=>MONGO_ROOT_PASSWORD],["MONGO_HOST",()=>MONGO_HOST],["MONGO_PORT",()=>MONGO_PORT],["MONGODB_USER",()=>MONGODB_USER],["MONGODB_PASSWORD",()=>MONGODB_PASSWORD],["MONGODB_DATABASE",()=>MONGODB_DATABASE],["IN_PROD",()=>IN_PROD]]);const {
  NODE_ENV,
  MONGO_ROOT_USER,
  MONGO_ROOT_PASSWORD,
  MONGO_HOST,
  MONGO_PORT,
  MONGODB_USER,
  MONGODB_PASSWORD,
  MONGODB_DATABASE,

} = process.env

       const IN_PROD = NODE_ENV === 'production'

/*
const {
  NODE_ENV
} = process.env

const IN_PROD = NODE_ENV === 'production'

module.exports = {
  NODE_ENV,
  IN_PROD,
}
*/
