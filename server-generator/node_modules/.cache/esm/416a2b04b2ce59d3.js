_b2c‍.x([["NODE_ENV",()=>NODE_ENV],["MONGO_HOST",()=>MONGO_HOST],["MONGO_PORT",()=>MONGO_PORT],["MONGODB_USER",()=>MONGODB_USER],["MONGODB_PASSWORD",()=>MONGODB_PASSWORD],["MONGODB_DATABASE",()=>MONGODB_DATABASE],["SECRET_KEY",()=>SECRET_KEY],["ACCESSTOKEN_EXPIRED",()=>ACCESSTOKEN_EXPIRED],["REFRESH_SECRET_KEY",()=>REFRESH_SECRET_KEY],["REFRESHTOKEN_EXPIRED",()=>REFRESHTOKEN_EXPIRED],["CLIENT_ADDR",()=>CLIENT_ADDR],["CLIENT_PORT",()=>CLIENT_PORT],["IN_PROD",()=>IN_PROD]]);const {
  NODE_ENV,
  MONGO_HOST,
  MONGO_PORT,
  MONGODB_USER,
  MONGODB_PASSWORD,
  MONGODB_DATABASE,

  SECRET_KEY,
  ACCESSTOKEN_EXPIRED,
  REFRESH_SECRET_KEY,
  REFRESHTOKEN_EXPIRED,

  CLIENT_ADDR,
  CLIENT_PORT
} = process.env

       const IN_PROD = NODE_ENV === 'production'

/*
const {
  NODE_ENV
} = process.env

const IN_PROD = NODE_ENV === 'production'

module.exports = {
  NODE_ENV,
  IN_PROD,
}
*/
